resources:
  jobs:
    bronze_etl:
      name: bronze_etl
      environments:
        - environment_key: default
          spec:
            environment_version: 3   # serverless runtime
            dependencies:
              - databricks-labs-dqx
      tasks:
        - task_key: transaction_data
          environment_key: default
          spark_python_task:
            python_file: ../../src/bronze/transaction_data.py
